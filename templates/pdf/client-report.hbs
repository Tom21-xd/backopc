<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: 'Segoe UI', 'Helvetica Neue', 'Arial', sans-serif;
            color: #1F2937;
            line-height: 1.6;
            background: #F9FAFB;
        }

        .logo-container {
            text-align: center;
            margin-bottom: 20px;
        }

        .logo {
            max-width: 180px;
            height: auto;
        }

        .header {
            background: linear-gradient(135deg, #1E3A8A 0%, #3B82F6 100%);
            color: white;
            padding: 35px 40px;
            text-align: center;
            border-radius: 12px;
            margin-bottom: 30px;
            box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
        }

        .header h1 {
            margin: 0;
            font-size: 28px;
            font-weight: 700;
            letter-spacing: -0.4px;
        }

        .header h2 {
            margin: 10px 0 0 0;
            font-size: 16px;
            opacity: 0.95;
            font-weight: 400;
        }

        .header p {
            margin: 8px 0 0 0;
            font-size: 14px;
            opacity: 0.85;
        }

        .info-section {
            background: white;
            padding: 25px;
            border-radius: 12px;
            margin-bottom: 25px;
            box-shadow: 0 2px 8px rgba(0,0,0,0.08);
        }

        .info-grid {
            display: grid;
            grid-template-columns: repeat(2, 1fr);
            gap: 18px;
            margin-top: 15px;
        }

        .info-item {
            padding: 15px;
            background: #F9FAFB;
            border-radius: 8px;
            border-left: 4px solid #1E3A8A;
        }

        .info-label {
            font-size: 12px;
            color: #6B7280;
            font-weight: 600;
            margin-bottom: 6px;
            text-transform: uppercase;
            letter-spacing: 0.5px;
        }

        .info-value {
            font-size: 16px;
            color: #1F2937;
            font-weight: 600;
        }

        .section-title {
            font-size: 22px;
            font-weight: 700;
            color: #1F2937;
            margin-bottom: 18px;
            padding-bottom: 10px;
            border-bottom: 3px solid #1E3A8A;
            letter-spacing: -0.3px;
        }

        .summary-grid {
            display: grid;
            grid-template-columns: repeat(4, 1fr);
            gap: 20px;
            margin-bottom: 30px;
        }

        .summary-card {
            background: white;
            padding: 22px;
            border-radius: 12px;
            box-shadow: 0 2px 8px rgba(0,0,0,0.08);
            text-align: center;
            border-top: 4px solid #1E3A8A;
        }

        .summary-value {
            font-size: 36px;
            font-weight: 700;
            color: #1E3A8A;
            margin-bottom: 8px;
        }

        .summary-label {
            font-size: 13px;
            color: #6B7280;
            text-transform: uppercase;
            letter-spacing: 0.8px;
            font-weight: 600;
        }

        .tank-section {
            background: white;
            padding: 25px;
            border-radius: 12px;
            margin-bottom: 25px;
            box-shadow: 0 2px 8px rgba(0,0,0,0.08);
            page-break-inside: avoid;
        }

        .tank-header {
            background: linear-gradient(135deg, #1E3A8A 0%, #2563EB 100%);
            color: white;
            padding: 15px 20px;
            border-radius: 8px;
            margin-bottom: 20px;
            display: flex;
            justify-content: space-between;
            align-items: center;
        }

        .tank-code {
            font-size: 20px;
            font-weight: 700;
        }

        .tank-status {
            padding: 6px 14px;
            border-radius: 20px;
            font-size: 12px;
            font-weight: 600;
            text-transform: uppercase;
            background: rgba(255,255,255,0.25);
        }

        .tank-stats {
            display: grid;
            grid-template-columns: repeat(3, 1fr);
            gap: 15px;
            margin-bottom: 20px;
        }

        .tank-stat-card {
            background: #F9FAFB;
            padding: 15px;
            border-radius: 8px;
            text-align: center;
        }

        .tank-stat-value {
            font-size: 24px;
            font-weight: 700;
            color: #1E3A8A;
            margin-bottom: 5px;
        }

        .tank-stat-label {
            font-size: 12px;
            color: #6B7280;
            text-transform: uppercase;
            letter-spacing: 0.5px;
        }

        table {
            width: 100%;
            border-collapse: collapse;
            background: white;
            box-shadow: 0 2px 6px rgba(0,0,0,0.08);
            margin-bottom: 20px;
            border-radius: 8px;
            overflow: hidden;
        }

        thead {
            background: linear-gradient(135deg, #1E3A8A 0%, #2563EB 100%);
            color: white;
        }

        th, td {
            padding: 12px;
            text-align: left;
            border-bottom: 1px solid #E5E7EB;
        }

        tbody tr:hover {
            background: #F9FAFB;
        }

        .status-badge {
            display: inline-block;
            padding: 5px 12px;
            border-radius: 20px;
            font-size: 11px;
            font-weight: 600;
            text-transform: uppercase;
        }

        .status-active {
            background: #D1FAE5;
            color: #065F46;
        }

        .status-inactive {
            background: #FECACA;
            color: #991B1B;
        }

        .status-completed {
            background: #D1FAE5;
            color: #065F46;
        }

        .status-scheduled {
            background: #FEF3C7;
            color: #92400E;
        }

        .level-critical {
            color: #DC2626;
            font-weight: 700;
        }

        .level-warning {
            color: #F59E0B;
            font-weight: 700;
        }

        .level-normal {
            color: #10B981;
            font-weight: 700;
        }

        .footer {
            margin-top: 50px;
            padding: 20px;
            background: #F9FAFB;
            border-top: 3px solid #1E3A8A;
            text-align: center;
            font-size: 12px;
            color: #6B7280;
            border-radius: 8px;
        }

        .page-break {
            page-break-after: always;
        }

        .subsection-title {
            font-size: 16px;
            font-weight: 600;
            color: #374151;
            margin: 20px 0 12px 0;
            padding-bottom: 6px;
            border-bottom: 2px solid #E5E7EB;
        }
    </style>
</head>
<body>
    <div class="logo-container">
        <img src="file:///C:/Users/USER/Desktop/U/Danna/opcDanna/backopc/src/img/gasCaqueta.png" alt="Gas Caquetá Logo" class="logo">
    </div>

    <div class="header">
        <h1>INFORME DE CLIENTE</h1>
        <h2>{{#if client.companyName}}{{client.companyName}}{{else}}{{client.firstName}} {{client.lastName}}{{/if}}</h2>
        <p>Período: {{period.start}} - {{period.end}}</p>
        <p>Generado el {{formatDate generatedDate}}</p>
    </div>

    <!-- Información del Cliente -->
    <div class="info-section">
        <h2 class="section-title">Información del Cliente</h2>
        <div class="info-grid">
            <div class="info-item">
                <div class="info-label">Nombre/Empresa</div>
                <div class="info-value">{{#if client.companyName}}{{client.companyName}}{{else}}{{client.firstName}} {{client.lastName}}{{/if}}</div>
            </div>
            <div class="info-item">
                <div class="info-label">Email</div>
                <div class="info-value">{{client.email}}</div>
            </div>
            <div class="info-item">
                <div class="info-label">Teléfono</div>
                <div class="info-value">{{client.phone}}</div>
            </div>
            <div class="info-item">
                <div class="info-label">Dirección</div>
                <div class="info-value">{{client.address}}</div>
            </div>
            <div class="info-item">
                <div class="info-label">Número de Tanques</div>
                <div class="info-value">{{client.tanksCount}}</div>
            </div>
        </div>
    </div>

    <!-- Resumen del Período -->
    <div class="info-section">
        <h2 class="section-title">Resumen del Período</h2>
        <div class="summary-grid">
            <div class="summary-card">
                <div class="summary-value">{{summary.totalConsumption}}</div>
                <div class="summary-label">Consumo Total (L)</div>
            </div>
            <div class="summary-card">
                <div class="summary-value">{{summary.totalSupplies}}</div>
                <div class="summary-label">Total Suministros</div>
            </div>
            <div class="summary-card">
                <div class="summary-value">{{summary.totalAlerts}}</div>
                <div class="summary-label">Total Alertas</div>
            </div>
            <div class="summary-card">
                <div class="summary-value">{{summary.totalRecharges}}</div>
                <div class="summary-label">Total Recargas</div>
            </div>
        </div>
    </div>

    <!-- Detalle por Tanque -->
    {{#each tanks}}
    <div class="page-break"></div>
    <div class="tank-section">
        <div class="tank-header">
            <div class="tank-code">TANQUE: {{this.tank.code}}</div>
            <div class="tank-status">{{this.tank.status}}</div>
        </div>

        <!-- Información del Tanque -->
        <div class="info-grid">
            <div class="info-item">
                <div class="info-label">Ubicación</div>
                <div class="info-value">{{this.tank.location}}</div>
            </div>
            <div class="info-item">
                <div class="info-label">Capacidad Total</div>
                <div class="info-value">{{liters this.tank.capacityLiters}}</div>
            </div>
            <div class="info-item">
                <div class="info-label">Nivel Actual</div>
                <div class="info-value">
                    <span class="{{#if (lt this.tank.currentLevelPercentage 10)}}level-critical{{else if (lt this.tank.currentLevelPercentage 20)}}level-warning{{else}}level-normal{{/if}}">
                        {{percentage this.tank.currentLevelPercentage}}
                    </span>
                </div>
            </div>
            <div class="info-item">
                <div class="info-label">Estado del Sensor</div>
                <div class="info-value">{{this.tank.sensor.status}}</div>
            </div>
        </div>

        <!-- Estadísticas del Tanque -->
        <h3 class="subsection-title">Estadísticas del Período</h3>
        <div class="tank-stats">
            <div class="tank-stat-card">
                <div class="tank-stat-value">{{this.statistics.totalConsumption}}</div>
                <div class="tank-stat-label">Consumo Total (L)</div>
            </div>
            <div class="tank-stat-card">
                <div class="tank-stat-value">{{this.statistics.totalRecharges}}</div>
                <div class="tank-stat-label">Recargas</div>
            </div>
            <div class="tank-stat-card">
                <div class="tank-stat-value">{{this.statistics.averageDailyConsumption}}</div>
                <div class="tank-stat-label">Consumo Diario (L)</div>
            </div>
        </div>

        <!-- Recargas del Tanque -->
        {{#if this.recharges}}
        <h3 class="subsection-title">Historial de Recargas (Últimas 10)</h3>
        <table>
            <thead>
                <tr>
                    <th>Fecha Programada</th>
                    <th>Fecha Completada</th>
                    <th>Cantidad</th>
                    <th>Estado</th>
                </tr>
            </thead>
            <tbody>
                {{#each this.recharges}}
                <tr>
                    <td>{{formatDate this.scheduledDate}}</td>
                    <td>{{#if this.completedDate}}{{formatDate this.completedDate}}{{else}}Pendiente{{/if}}</td>
                    <td>{{liters this.actualQuantityLiters}}</td>
                    <td>
                        <span class="status-badge status-{{this.status}}">
                            {{this.status}}
                        </span>
                    </td>
                </tr>
                {{/each}}
            </tbody>
        </table>
        {{/if}}

        <!-- Alertas del Tanque -->
        {{#if this.alerts}}
        <h3 class="subsection-title">Alertas Recientes (Últimas 10)</h3>
        <table>
            <thead>
                <tr>
                    <th>Fecha</th>
                    <th>Tipo</th>
                    <th>Severidad</th>
                    <th>Mensaje</th>
                    <th>Estado</th>
                </tr>
            </thead>
            <tbody>
                {{#each this.alerts}}
                <tr>
                    <td>{{formatDate this.createdAt}}</td>
                    <td>{{this.type}}</td>
                    <td>{{alertSeverity this.severity}}</td>
                    <td>{{this.message}}</td>
                    <td>{{alertStatus this.status}}</td>
                </tr>
                {{/each}}
            </tbody>
        </table>
        {{/if}}
    </div>
    {{/each}}

    <div class="footer">
        <p><strong>Gas Caquetá - Sistema de Monitoreo de Tanques de Gas</strong></p>
        <p>Informe de Cliente - Generado automáticamente</p>
        <p>{{formatDate generatedDate}}</p>
        <p>Este documento contiene información confidencial del cliente</p>
    </div>
</body>
</html>
